[package]
name = "redox_perf_annotations"
version = "0.1.0"
edition = "2024"
description = "Compact performance annotations: @pi! (inline), @pnb (no bounds check), @pv(N) (vectorize), @pt(target) (target placement)"

[dependencies]
