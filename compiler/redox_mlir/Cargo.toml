[package]
name = "redox_mlir"
version = "0.1.0"
edition = "2021"
description = "MLIR integration for the Redox compiler — FFI bindings and safe wrappers"
license = "MIT OR Apache-2.0"

[lib]
name = "redox_mlir"
path = "src/lib.rs"

[dependencies]

[features]
# Enable actual MLIR C API FFI linking.
# Without this feature, a mock implementation is used for testing.
mlir_ffi = []
