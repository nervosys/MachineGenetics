name: redox-pull

on:
  workflow_dispatch:
  schedule:
    # Run at 04:00 UTC every Monday
    - cron: '0 4 * * 1'

jobs:
  pull:
    if: github.repository == 'rust-lang/redox-dev-guide'
    uses: rust-lang/josh-sync/.github/workflows/redox-pull.yml@main
    with:
      github-app-id: ${{ vars.APP_CLIENT_ID }}
      zulip-stream-id: 196385
      zulip-bot-email:  "redox-dev-guide-gha-notif-bot@rust-lang.zulipchat.com"
      pr-base-branch: main
      branch-name: redox-pull
    secrets:
      zulip-api-token: ${{ secrets.ZULIP_API_TOKEN }}
      github-app-secret: ${{ secrets.APP_PRIVATE_KEY }}
