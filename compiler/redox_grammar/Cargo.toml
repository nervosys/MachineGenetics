[package]
name = "redox_grammar"
version = "0.0.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[workspace]
description = """
Formal LL(1) grammar specification for Redox canonical syntax.
Serves as the single source of truth for the parser.
"""

[lib]

[dependencies]

[dev-dependencies]
